#include "io/MeshLoader.h"

namespace te
{
    Mesh* MeshLoader::readObjFile(const std::string& filename)
    {
        // using 3rd party lib to read
        // no need to write it ourselves now
    }
}